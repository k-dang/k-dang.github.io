---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<div>
  <h2 class="font-display text-sm font-semibold tracking-tight text-muted-foreground mb-2">{title}</h2>
  <slot />
</div>
